name: Deploy

on: ['deployment']

jobs:
  deployment:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo 'run tests'$a
      - name: 'Deploy ${{ github.event.deployment.environment }}'
        run: |
          echo 'YOUR CODE HERE'      